package day03;

public class 연산자1 {

	public static void main(String[] args) {
		
		
		// 연산자 : 연산을 수행하면 연산의 결과가 발생한다
		
		
		// 단항연산자 : -n; ++n, n++ , --n, n--
		// 이항연산자 : a+b, a-b, a*b, a/b, a%b,
		// 삼항연산자 : (조건) ? 참 : 거짓;
		// 대입연산자 : = (변수에 값을 기억할 때)
		// 복합대입연산자 : += , -=, *=, /=, %= (sum = sum + i => sum += i)
		// 논리연산자 : &&(and), ||(or), !(not)
	}

}
